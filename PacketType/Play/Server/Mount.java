/* This packet is sent every time you mount or unmount an entity */
                
mount.getIntegers().read(0); // Entity ID
mount.getEntityModifier(world).read(0); // Entity
mount.getIntegerArrays().read(0); // Passenger IDs
